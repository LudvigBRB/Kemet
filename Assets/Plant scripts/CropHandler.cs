/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CropHandler : MonoBehaviour
{
    public GameObject cylinder;

    // Start is called before the first frame update
    /*
    void PlantCrop()
    {
        Vector3 clickPosition =
      Camera.main.ScreenToWorldPoint(Input.mousePosition);

        clickPosition.z = clickPosition.z - 1;
        clickPosition.x = clickPosition.x - 3;

        Instantiate(cylinder, clickPosition, Quaternion.identity);
        Debug.Log(clickPosition);
    }

    

void PlantCrop()
{
    GameObject player = GameObject.Find("Player");
    Transform playerTransform = player.transform;
    Vector3 position = playerTransform.position;

    position.x = position.x + 3;
    position.z = position.z + 3;

    Instantiate(cylinder, position, Quaternion.identity);
}

// Update is called once per frame
void Update()
{
    if (Input.GetMouseButtonDown(0))
        PlantCrop();
}
}
*/